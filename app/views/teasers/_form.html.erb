<%= form_for(@teaser) do |f| %>
  <% if @teaser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teaser.errors.count, "error") %> prohibited this teaser from being saved:</h2>

      <ul>
      <% @teaser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_area :answer %>
  </div>
  <div class="field">
    <%= f.label :piece %><br />
    <%= f.text_field :piece %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
